import {authenticate,setSession} from "./auth.js";import { $,toast } from "./utils.js";import {createCaptcha,captchaValid} from "./captcha.js";import {verifyAdminPin} from "../firebase/auth.js";
createCaptcha($("#captchaQuestion"));$("#refreshCaptcha").onclick=()=>createCaptcha($("#captchaQuestion"));
document.querySelectorAll(".password-toggle").forEach(b=>b.onclick=()=>{const i=$("#"+b.dataset.target);i.type=i.type==="password"?"text":"password";b.textContent=i.type==="password"?"Tampilkan":"Sembunyikan"});
$("#loginForm").onsubmit=async e=>{e.preventDefault();const m=$("#formMessage");m.textContent="";try{if(!captchaValid($("#captchaAnswer").value))throw new Error("Captcha salah.");const r=await authenticate($("#email").value,$("#password").value);if(r.type==="admin"){const pin=prompt("Masukkan PIN Admin");if(!(await verifyAdminPin(r.uid,pin||"")))throw new Error("PIN admin salah.");setSession({type:"admin",uid:r.uid,email:r.email,loginAt:Date.now()});location.href="pages/admin.html"}else{setSession({type:"user",userId:r.user.uid,uid:r.user.uid,loginAt:Date.now()});location.href="pages/user.html"}}catch(err){m.textContent=err.message||"Login gagal.";m.className="form-message error";createCaptcha($("#captchaQuestion"));$("#captchaAnswer").value=""}};
