function toast(t){const x=document.createElement("div");x.className="toast";x.textContent=t;document.body.appendChild(x);setTimeout(()=>x.remove(),2200)}function layout(title,links,user){const nav=links.map(([n,h,i])=>`<a href="${h}">${i} ${n}</a>`).join("");return `<div class="shell"><aside class="sidebar"><a class="brand" href="dashboard.html"><span>GC</span> GameConfig</a><nav>${nav}<a href="#" onclick="logout()">↪ Keluar</a></nav></aside><main class="main"><div class="page-title"><div><div class="sub">${user}</div><h1>${title}</h1></div></div></main><nav class="mobile-nav">${nav}</nav></div>`}