const u=requireAuth("admin");function render(){const a=Storage.get("configs",CONFIGS);document.getElementById("app").innerHTML=layout("▣ Config Manager",[["Dashboard","dashboard.html","⌂"],["Users","users.html","♙"],["Configs","configs.html","▣"],["Settings","settings.html","⚙"]],u.name);document.querySelector(".main").insertAdjacentHTML("beforeend",`<div class="panel"><div class="row"><div><h2>Preset</h2><p class="sub">Kelola preset referensi.</p></div><button class="btn primary" onclick="addConfig()">+ Tambah</button></div><table class="table"><thead><tr><th>Nama</th><th>Game</th><th>Kategori</th><th>Aksi</th></tr></thead><tbody>${a.map(x=>`<tr><td>${x.name}</td><td>${x.game}</td><td>${x.category}</td><td><button class="btn danger" onclick="del('${x.id}')">Hapus</button></td></tr>`).join("")}</tbody></table></div>`)}function del(id){if(confirm("Hapus preset?")){Storage.set("configs",Storage.get("configs",CONFIGS).filter(x=>x.id!==id));render()}}function addConfig(){const name=prompt("Nama preset:");if(!name)return;const game=prompt("Game (free-fire/mobile-legends):","free-fire");const a=Storage.get("configs",CONFIGS);a.push({id:"c"+Date.now(),game,name,category:"Custom",values:{General:80,Control:80,Precision:80}});Storage.set("configs",a);render()}render();