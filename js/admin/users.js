const u=requireAuth("admin");function render(){const users=Storage.get("gch_users",[]);document.getElementById("app").innerHTML=layout("♙ Users",[["Dashboard","dashboard.html","⌂"],["Users","users.html","♙"],["Configs","configs.html","▣"],["Settings","settings.html","⚙"]],u.name);document.querySelector(".main").insertAdjacentHTML("beforeend",`<div class="panel"><div class="row"><h2>Daftar User</h2><button class="btn primary" onclick="addUser()">+ User</button></div><table class="table"><thead><tr><th>Username</th><th>Nama</th><th>Role</th><th>Status</th><th>Aksi</th></tr></thead><tbody>${users.map(x=>`<tr><td>${x.username}</td><td>${x.name}</td><td>${x.role}</td><td>${x.active?"Aktif":"Nonaktif"}</td><td><button class="btn ghost" onclick="toggle('${x.id}')">Toggle</button></td></tr>`).join("")}</tbody></table></div>`)}function toggle(id){let a=Storage.get("gch_users",[]),x=a.find(x=>x.id===id);if(x.username!=="admin")x.active=!x.active;Storage.set("gch_users",a);render()}function addUser(){const username=prompt("Username baru:");if(!username)return;const a=Storage.get("gch_users",[]);if(a.some(x=>x.username===username))return alert("Username sudah ada.");a.push({id:"u"+Date.now(),username,password:"user123",role:"user",name:username,active:true});Storage.set("gch_users",a);render()}render();