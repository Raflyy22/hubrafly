import {escapeHTML,formatNumber} from "../core/utils.js";
export function scriptCard(s,isVip=false){return `<article class="script-card glass"><div class="script-cover"><div class="script-game-mark ${s.gameId}">${escapeHTML(s.game==="Free Fire"?"FF":"ML")}</div><span class="game-label">${escapeHTML(s.game)}</span>${isVip?'<span class="vip-tag">VIP</span>':""}</div><div class="script-body"><div class="script-meta">${escapeHTML(s.category)} · ${escapeHTML(s.subCategory)}</div><h3>${escapeHTML(s.name)}</h3><p>${escapeHTML(s.description||"Tidak ada deskripsi.")}</p><div class="script-stats"><span>♡ ${formatNumber(s.likes)}</span><span>◉ ${formatNumber(s.views)}</span><span>v${escapeHTML(s.version)}</span></div><a class="btn btn-ghost btn-block" href="detail.html?id=${encodeURIComponent(s.id)}">Lihat detail →</a></div></article>`}
